// Configuração do ambiente de produção
// Para alterar a URL da API, modifique o valor de apiBaseUrl
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:5189/api' // URL base da API backend - ajustar para produção
};
